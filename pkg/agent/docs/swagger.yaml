definitions:
  config.SyncConfigRequest:
    properties:
      configmap_name:
        type: string
      namespace:
        type: string
    required:
    - configmap_name
    - namespace
    type: object
  config.SyncConfigResponse:
    properties:
      message:
        type: string
    type: object
host: 127.0.0.1:22104
info:
  contact:
    email: yuzhenhao0906@gmail.com
    name: yuzhenhao
    url: http://bsgchind.io
  description: swagger test
  title: unit-agent api demo
  version: "1.0"
paths:
  /config/sync:
    post:
      consumes:
      - application/json
      description: In the kubernetes cluster to obtain the contents of the ConfigMap, the use of confd landing generated into the corresponding software format configuration file
      parameters:
      - description: ConfigMap info
        in: body
        name: SyncConfigRequest
        required: true
        schema:
          $ref: '#/definitions/config.SyncConfigRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/config.SyncConfigResponse'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/config.SyncConfigResponse'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/config.SyncConfigResponse'
      summary: interface of sync config
      tags:
      - interface of sync config
swagger: "2.0"