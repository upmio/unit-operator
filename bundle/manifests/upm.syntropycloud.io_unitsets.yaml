apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  creationTimestamp: null
  name: unitsets.upm.syntropycloud.io
spec:
  conversion:
    strategy: Webhook
    webhook:
      clientConfig:
        service:
          name: unit-operator-webhook-service
          namespace: upm-system
          path: /convert
      conversionReviewVersions:
      - v1
  group: upm.syntropycloud.io
  names:
    kind: UnitSet
    listKind: UnitSetList
    plural: unitsets
    shortNames:
    - us
    singular: unitset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: TYPE
      priority: 1
      type: string
    - jsonPath: .spec.version
      name: VERSION
      priority: 1
      type: string
    - jsonPath: .spec.units
      name: EXPECTED
      type: integer
    - jsonPath: .status.units
      name: CURRENT
      type: integer
    - jsonPath: .status.readyUnits
      name: READY
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: UnitSet is the Schema for the unitsets API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UnitSetSpec defines the desired state of UnitSet
            properties:
              certificateProfile:
                description: CertificateProfile defines the configuration for certificate
                  profile
                properties:
                  organizations:
                    description: Organizations List of organization names
                    items:
                      type: string
                    type: array
                  root_secret:
                    description: RootSecret Root secret name
                    type: string
                type: object
              edition:
                description: Edition of the unit set
                type: string
              emptyDir:
                description: EmptyDir defines the configuration for emptyDir
                items:
                  properties:
                    mountPath:
                      description: MountPath Mount path
                      type: string
                    name:
                      description: Name of the storage
                      type: string
                    size:
                      description: Size of the storage
                      type: string
                  type: object
                type: array
              env:
                description: Env Environment variables for the units
                x-kubernetes-preserve-unknown-fields: true
              externalService:
                description: ExternalService Configuration for external services
                properties:
                  type:
                    description: Type of the external service (e.g., NodePort)
                    type: string
                type: object
              extraVolume:
                x-kubernetes-preserve-unknown-fields: true
              nodeAffinityPreset:
                description: NodeAffinityPreset  Node affinity rules
                items:
                  description: NodeAffinityPresetSpec defines node affinity rules.
                  properties:
                    key:
                      description: Key for the node affinity
                      type: string
                    values:
                      description: Values for the node affinity
                      items:
                        type: string
                      type: array
                  type: object
                type: array
              podAntiAffinityPreset:
                description: PodAntiAffinityPreset Pod anti-affinity policy
                type: string
              podMonitor:
                description: PodMonitor defines the configuration for pod monitor
                properties:
                  enable:
                    description: |-
                      Enable define if need create pod monitor
                      default: false
                    type: boolean
                type: object
              resources:
                description: Resources Resource requirements for the units
                x-kubernetes-preserve-unknown-fields: true
              storage:
                description: Storages defines the configuration for storage
                items:
                  description: StorageSpec defines the configuration for storage.
                  properties:
                    mountPath:
                      description: MountPath Mount path
                      type: string
                    name:
                      description: Name of the storage
                      type: string
                    size:
                      description: Size of the storage
                      type: string
                    storageClassName:
                      description: StorageClassName storage class name
                      type: string
                  type: object
                type: array
              type:
                description: Type is the type of the unitset
                type: string
              unitService:
                description: UnitService Configuration for unit services
                properties:
                  type:
                    description: Type of the unit service (e.g., ClusterIP)
                    type: string
                type: object
              units:
                description: Units Number of units in the unitset
                type: integer
              updateStrategy:
                description: UpdateStrategy Strategy for updating the unit set
                properties:
                  rollingUpdate:
                    description: RollingUpdate Rolling update configuration
                    properties:
                      maxUnavailable:
                        description: MaxUnavailable Maximum number of unavailable
                          pods during update
                        format: int32
                        type: integer
                      partition:
                        description: Partition Number of partitions for the update
                        format: int32
                        type: integer
                    type: object
                  type:
                    description: Type of update strategy (e.g., RollingUpdate)
                    type: string
                type: object
              version:
                description: Version of the unit set, e.g.:8.0.40
                type: string
            type: object
          status:
            description: UnitSetStatus defines the observed state of UnitSet
            properties:
              conditions:
                description: Conditions is an array of conditions.
                x-kubernetes-preserve-unknown-fields: true
              externalService:
                description: ExternalService the information of unitset external service
                properties:
                  name:
                    description: Name the name of unitset external service
                    type: string
                type: object
              inUpdate:
                description: InUpdate used to mark if a mirror upgrade or resource
                  change is in progress
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed for this UnitSet. It corresponds to the
                  UnitSet's generation, which is updated on mutation by the API Server.
                format: int64
                type: integer
              readyUnits:
                description: ReadyUnits the number of ready units
                type: integer
              unitImageSynced:
                description: ImageSyncStatus defines the status of the image sync
                properties:
                  lastTransitionTime:
                    description: LastTransitionTime the last transition time
                    format: date-time
                    type: string
                  status:
                    description: |-
                      Status the status of the image sync
                      enum: True, False
                    type: string
                type: object
              unitPVCSynced:
                description: PvcSyncStatus defines the status of the pvc sync
                properties:
                  lastTransitionTime:
                    description: LastTransitionTime the last transition time
                    format: date-time
                    type: string
                  status:
                    description: |-
                      Status the status of the pvc sync
                      enum: True, False
                    type: string
                type: object
              unitResourceSynced:
                description: ResourceSyncStatus defines the status of the resource
                  sync
                properties:
                  lastTransitionTime:
                    description: LastTransitionTime the last transition time
                    format: date-time
                    type: string
                  status:
                    description: |-
                      Status the status of the resource sync
                      enum: True, False
                    type: string
                type: object
              unitService:
                description: UnitService the information of unit service
                properties:
                  name:
                    additionalProperties:
                      type: string
                    description: Name which is a map, the key is unit name, the value
                      is unit service name
                    type: object
                type: object
              units:
                description: Units the number of units
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: null
  storedVersions: null
